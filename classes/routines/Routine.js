const BindBotBase = require("./../BindBotBase.js");
/**
 * 
 * Base class for our routines. Will have basic generic function that will help handle tasks
 * 
 */
module.exports = class Routine extends BindBotBase {
    constructor(bot) {
        super(bot);
    }
}