const BindBotBase = require("./../BindBotBase.js");
/**
 * 
 * Base class for our Tasks. It will have basic function to help with the bot controls
 * 
 */
module.exports = class Task extends BindBotBase {
    constructor(bot) {
        super(bot);
    }
}